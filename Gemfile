source "https://rubygems.org"

# GitHub Pages builds this site natively — the gem is only needed for a local
# preview (`bundle exec jekyll serve`). No themes or plugins are required.
gem "github-pages", group: :jekyll_plugins
